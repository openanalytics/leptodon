[workspace]
[package]
name = "overview-codegen"
edition = "2024"

[dependencies]
leptodon = { path = "../../leptodon", default-features = false, features = [] }
syn = { version = "2.0.117", features = ["parsing"] }
