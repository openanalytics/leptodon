[package]
name = "codegen"
version = "1.0.0"
edition = "2024"

[dependencies]
leptodon = { path = "../..", default-features = false, features = [] }
syn = { version = "2.0.117", features = ["full"] }
