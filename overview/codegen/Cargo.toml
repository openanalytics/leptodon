[package]
name = "codegen"
version = "0.1.0"
edition = "2024"

[dependencies]
leptodon = { path = "../..", default-features = false, features = [] }
syn = { version = "2.0.110", features = ["full"] }
